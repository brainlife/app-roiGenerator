#!/bin/bash
#PBS -l nodes=1:ppn=1,vmem=16gb,walltime=1:00:00
#PBS -N roiMerge
#PBS -V

echo "merging ROIs"
time singularity exec -e docker://brainlife/afni:16.3.0 ./roiMerge.sh

#if [ -z "$(ls -A ./rois/rois/)" ]; then
#    echo "ROI files missing."
#    exit 1
#else
#    exit 0
#fi
